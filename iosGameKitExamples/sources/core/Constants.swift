import SpriteKit

let showStatistics = true

let sceneBackgroundColor = SKColor.black
let sceneAnchorPoint = CGPoint(x: 0.5, y: 0.5)

let buttonColor = SKColor.green
let buttonSize = CGSize(width: 150, height: 50)
